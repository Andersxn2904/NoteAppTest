services:
  notasapp:
    build:
      context: ./NoteAppCore
      dockerfile: Dockerfile
    container_name: notasapp
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
